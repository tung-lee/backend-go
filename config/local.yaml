server:
  port: 8080
databases:
  - user: admin
    password: admin
    host: localhost
    dbName: shopDev
  - user: order
    password: order
    host: localhost
    dbName: order
security:
  jwt:
    secret: "secret"
redis:
  user: admin
elastisearch:
  user: admin
rabbitMQ:
  user: admin
kafka:
  user: admin
